% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairwiseDIF.R
\name{pairwiseDIF}
\alias{pairwiseDIF}
\title{Pairwise post hoc DIF analysis}
\usage{
pairwiseDIF(
  object = NULL,
  dat = NULL,
  Q = NULL,
  group = NULL,
  model = "GDINA",
  sequential = FALSE,
  dif.items = NULL,
  anchor.items = NULL,
  alpha.level = 0.05,
  p.adjust.methods = "holm",
  SE.type = NULL,
  ...
)
}
\arguments{
\item{object}{an object returned by \code{dif()}.}

\item{dat}{item responses from two or more groups; missing data need to be coded as \code{NA}.}

\item{Q}{Q-matrix specifying the association between items and attributes.}

\item{group}{a factor or a vector indicating the group each individual belongs to.}

\item{model}{model for each item.}

\item{sequential}{Logical; whether a sequential model is fit to the data. Default is \code{FALSE}.}

\item{dif.items}{which items are subject to pairwise DIF follow-up.}

\item{anchor.items}{optional anchor items. If omitted, all non-DIF items are treated as shared across groups.}

\item{alpha.level}{adjusted omnibus p-value cutoff used to flag DIF items when \code{object} is supplied.}

\item{p.adjust.methods}{adjusted p-values for pairwise Wald tests within each item.}

\item{SE.type}{Type of standard error estimation methods for the Wald test.}

\item{...}{arguments passed to \code{GDINA()} for the final post hoc refit.}
}
\value{
A \code{pairwiseDIF} object containing the pairwise Wald test table and final post hoc fit.
}
\description{
Conduct pairwise Wald follow-up tests after omnibus DIF analysis
with three or more groups.
The input can be a \code{dif} object returned by \code{dif()}, in which case
DIF items are selected using adjusted omnibus p-values, or raw data plus
user-specified DIF items and optional anchor items. \code{plot()} function
can be used to draw grouped bar chart for DIF items.
}
\examples{
\dontrun{

set.seed(123456)
N <- 500
Q <- sim30GDINA$simQ
gs <- matrix(.2,ncol = 2, nrow = nrow(Q))
# By default, individuals are simulated from uniform distribution
# and deltas are simulated randomly
sim1 <- simGDINA(N,Q,gs.parm = gs,model="DINA")
sim2 <- simGDINA(N,Q,gs.parm = gs,model=c(rep("DINA",nrow(Q)-1),"DINO"))
sim3 <- simGDINA(N,Q,gs.parm = gs,model="DINA")
dat <- rbind(extract(sim1,"dat"),extract(sim2,"dat"),extract(sim3,"dat"))
gr <- rep(c("G1","G2","G3"),each=N)

# DIF using Wald test - omnibus test
dif.wald <- dif(dat, Q, group=gr, method = "Wald")
dif.wald
# pairwise comparison between all pair of groups
dif.pair = pairwiseDIF(dif.wald)
dif.pair
# draw plot to show the DIF
plot(dif.pair, withSE = TRUE)
}
}
\seealso{
\code{\link{dif}}
}
\author{
Wenchao Ma, The University of Minnesota, \email{wma@umn.edu}
}
